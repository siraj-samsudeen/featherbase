## 1. Pin the normalization boundary

- [ ] 1.1 Add asymmetric server tests proving an unslashed app root preserves its
  encoded query while a query-free root keeps its existing canonical redirect.

## 2. Correct and govern the route

- [ ] 2.1 Preserve query state in the shared runtime-root slash normalization.
- [ ] 2.2 Sync and archive the scenario after focused route tests, strict OpenSpec,
  STC, evidence, typecheck, and diff checks pass.
