## 1. Contract and red tests

- [x] 1.1 Strictly validate proposal/design/delta/tasks before implementation; review spec with five axes.
- [x] 1.2 Add failing asymmetric identity/preview and transaction/activation tests bound to descriptive spec slugs.

## 2. Runtime upgrade implementation

- [x] 2.1 Implement strict cumulative typed migration validation and deterministic plan; prove malformed, drift, skip and downgrade rejection.
- [x] 2.2 Implement generic durable ledger, exact-version discovery, transactional additive DDL and separate activation under lifecycle locking; prove failures/retries/restart and disabled semantics.
- [x] 2.3 Add manager preview/upgrade/activate API and stale-client version admission; prove permission denial and queued obsolete requests.

## 3. Package and end-to-end proof

- [x] 3.1 Preserve Tasker 0.0.1 artifact and generate v2 fixture adding nullable Project.description; compare fresh and upgraded schemas with asymmetric work/grants/preferences.
- [x] 3.2 Extend literal separately-built-package proof with actual restart and browser requests; inspect results without touching development databases.
- [x] 3.3 Run affected/full server/shared suites, typechecks, strict OpenSpec/STC/policy and diff checks; perform code eight-axis and test three-axis reviews over affected area.

## 4. Handoff

- [x] 4.1 Document operator artifact delivery, recovery boundary, exact safe later Dev sequence and integration overlaps; verify commands against implemented API.
- [x] 4.2 Archive only after semantic verification, commit reviewable stages and leave worktree clean; report proof gaps and independent review requirement honestly.
