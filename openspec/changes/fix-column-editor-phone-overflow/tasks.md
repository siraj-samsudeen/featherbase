# Tasks

## 1. Contain the Columns table

- [x] 1.1 Add the existing card-level horizontal scroll pattern around the Columns editor table and verify the focused phone browser test passes.

## 2. Prove responsive behavior

- [x] 2.1 Add focused 375px browser coverage that verifies the Admin canvas does not overflow, the table scrolls inside its own area, and Rename can be reached without submitting a schema change; verify the focused test passes.
- [x] 2.2 Inspect the focused route at phone and desktop widths, capture a representative phone screenshot, and run the web typecheck and OpenSpec validation.
